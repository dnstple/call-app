/**
 * Branding — the ONE place the product name lives. The name may still
 * change; nothing else should hardcode it.
 */
export const APP_NAME = 'Apricoti';

/** Where "Contact us" / support links go. Change this to your real address. */
export const CONTACT_EMAIL = 'support@apricoti.com';
